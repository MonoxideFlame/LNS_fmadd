# What the point is:

The hope is to use PWL interpolation to obviate the need for a multiplier circuit in LNA arithmetic for 12 bit arithmetic, compared to fp8. The question is ultimately whether the extra lookup tables overshoot the area of a 12x12 multiplier and FP edge case handling.






